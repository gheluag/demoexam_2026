<Window
    x:Class="ShoeShop.Windows.LoginWin"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:ShoeShop.Windows"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="ООО &quot;Обувь&quot;"
    Width="450"
    Height="350"
    WindowStartupLocation="CenterScreen"
    mc:Ignorable="d">
    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">

        <Image Width="50" Source="/Resources/Icon.ico" />
        <TextBlock
            Margin="5"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            FontWeight="Bold"
            Text="Авторизация" />


        <Grid Margin="5">
            <TextBox x:Name="LoginTB" TextChanged="LoginTB_TextChanged" />

            <TextBlock
                x:Name="LoginPlaceholder"
                Margin="12,0,0,0"
                VerticalAlignment="Center"
                Foreground="Gray"
                IsHitTestVisible="False"
                Text="Email" />
        </Grid>

        <Grid Margin="5">
            <PasswordBox x:Name="PasswTB" PasswordChanged="PasswTB_PasswordChanged" />

            <TextBlock
                x:Name="PasswPlaceholder"
                Margin="12,0,0,0"
                VerticalAlignment="Center"
                Foreground="Gray"
                IsHitTestVisible="False"
                Text="Пароль" />
        </Grid>
        <Button
            x:Name="LoginBtn"
            Margin="10"
            Click="LoginBtn_Click"
            Content="Войти"
            Cursor="Hand"
            Style="{StaticResource LoginBtn}" />
        <TextBlock
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            Cursor="Hand"
            MouseLeftButtonUp="TextBlock_MouseLeftButtonUp"
            Text="Посмотреть товары" />

    </StackPanel>
</Window>
